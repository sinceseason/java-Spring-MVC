package com.xty.platform.controller.interceptor;import com.xty.common.JsonOpera;import com.xty.platform.util.Definition;import com.xty.result.Result;import org.springframework.web.servlet.HandlerInterceptor;import org.springframework.web.servlet.ModelAndView;import javax.servlet.http.HttpServletRequest;import javax.servlet.http.HttpServletResponse;import java.util.Date;/** * @author Eugene */public class SessionInterceptor implements HandlerInterceptor {    private JsonOpera jo = new JsonOpera();    public void afterCompletion(HttpServletRequest request,                                HttpServletResponse response, Object handler, Exception arg3)            throws Exception {    }    public void postHandle(HttpServletRequest request,                           HttpServletResponse response, Object arg2, ModelAndView arg3)            throws Exception {    }    public boolean preHandle(HttpServletRequest request,                             HttpServletResponse response, Object handler) throws Exception {//        System.out.println(request.getRequestURL()+"interceptor begin time ="+new Date());        Result rs = new Result();        try {            Object obj = request.getSession().getAttribute(request.getHeader("token"));            if (obj == null ) {                rs.setResult(Definition.FAILED);                rs.setErrorCode(Definition.HAVE_NOT_LOGINED_USER);//                rs.setDecodeData(request);                response.getWriter().print(jo.generateJsonString(rs));//                System.out.println(request.getRequestURL()+"interceptor end1 time =" + new Date());                    return false;            } else {//                System.out.println(request.getRequestURL()+"interceptor end2 time =" + new Date());                return true;            }        } catch (Exception ex) {            rs.setResult(Definition.FAILED);            rs.setData(ex.toString());            rs.setErrorCode(Definition.UNKNOW);            response.getWriter().print(jo.generateJsonString(rs));            System.out.println(request.getRequestURL()+"interceptor end3 time =" + new Date());            return false;        }    }}